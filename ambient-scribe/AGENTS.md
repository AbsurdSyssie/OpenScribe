# Ambient Scribe Bootstrap Notes

- Preserve `owner_user_id` and `team_id` on transcript-derived records.
- Do not add any transcript content sharing or admin content visibility.
- Keep setup/docs/tests in sync when changing the bootstrap.
