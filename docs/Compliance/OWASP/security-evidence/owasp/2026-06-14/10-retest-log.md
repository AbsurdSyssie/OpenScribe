# 10 - Retest Log

Status: retest log active. Public-form CSRF and header hardening retests recorded for 2026-06-14. Local authenticated dev-account crawl added 2026-06-15.

| Finding ID | Retest date | Environment | Tool/command | Role/account | Result | Evidence location | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OWASP-2026-06-14-011 | 2026-06-14 | Production public URL: `https://openscribe.co.uk` | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-2026-06-14.html -J zap-baseline-retest-2026-06-14.json -w zap-baseline-retest-2026-06-14.md -I` | Anonymous/public; ZAP default synthetic form values only. | Passed. ZAP output reported `PASS: Absence of Anti-CSRF Tokens [10202]`; retest report shows public form node names include `_csrf_token` for `/forgot-password`, `/login`, and `/request-access`. | `07-tool-outputs/zap/zap-baseline-retest-2026-06-14.md`, `.json`, `.html` | No cookies, tokens, account data, transcript/note content, prompts, provider responses, or audio committed. Other pre-existing warning classes remain open under separate findings/remediation items. |
| OWASP-2026-06-14-010 | 2026-06-14 | Production public URL: `https://openscribe.co.uk` | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-headers-2026-06-14.html -J zap-baseline-retest-headers-2026-06-14.json -w zap-baseline-retest-headers-2026-06-14.md -I` | Anonymous/public; ZAP default synthetic form values only. | Passed. ZAP output reported `PASS: Permissions Policy Header Not Set [10063]` and `PASS: Insufficient Site Isolation Against Spectre Vulnerability [90004]`; sampled `/login` returned `Permissions-Policy` and `Cross-Origin-Embedder-Policy: credentialless`. | `07-tool-outputs/zap/zap-baseline-retest-headers-2026-06-14.md`, `.json`, `.html` | No sensitive data committed. This closes the original missing `Permissions-Policy`/COEP finding. |
| OWASP-2026-06-14-012 | 2026-06-14 | Production public URL: `https://openscribe.co.uk` | Same ZAP command as header retest plus Python stdlib header sample for `/login` and `/static/vendor/lucide/1.8.0/lucide.min.js`. | Anonymous/public. | Partially passed. Duplicate-HSTS warning is gone, and `/login` sampled one Cloudflare HSTS header. ZAP now reports `Strict-Transport-Security Header Not Set` for one static JS asset. | `07-tool-outputs/zap/zap-baseline-retest-headers-2026-06-14.md`, `.json`, `.html` | Next action: ensure static asset responses also get one HSTS header from Cloudflare/proxy or app, then rerun ZAP. |
| OWASP-2026-06-14-012 | 2026-06-14 | Production public URL: `https://openscribe.co.uk` after Cloudflare HSTS update | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-hsts-cloudflare-2026-06-14.html -J zap-baseline-retest-hsts-cloudflare-2026-06-14.json -w zap-baseline-retest-hsts-cloudflare-2026-06-14.md -I`; Python stdlib header samples for `/login` and `/static/vendor/lucide/1.8.0/lucide.min.js`. | Anonymous/public. | Passed. Dynamic and static samples both returned one Cloudflare HSTS header: `max-age=15552000; includeSubDomains`. ZAP reported `PASS: Strict-Transport-Security Header [10035]` with no duplicate or missing HSTS warning. | `07-tool-outputs/zap/zap-baseline-retest-hsts-cloudflare-2026-06-14.md`, `.json`, `.html` | Keep production app at `HSTS_SOURCE=proxy` while Cloudflare owns HSTS globally. No cookies, tokens, content, or secrets committed. |
| OWASP-2026-06-14-013 | 2026-06-14 | Production public URL: `https://openscribe.co.uk` after metadata deploy | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-metadata-2026-06-14.html -J zap-baseline-retest-metadata-2026-06-14.json -w zap-baseline-retest-metadata-2026-06-14.md -I`; Python stdlib header/body samples for `/robots.txt`, `/.well-known/security.txt`, `/sitemap.xml`, and static JS. | Anonymous/public. | Partially passed. `/robots.txt` returned `200`, `security.txt` returned `200`, `/sitemap.xml` returned intentional `404`, and none set cookies. ZAP saw 20 URLs and no metadata redirect-to-login issue. Follow-up: `robots.txt` used `Allow: /$`, which caused ZAP to crawl literal `/$`; code changed to `Allow: /` and needs redeploy/retest. | `07-tool-outputs/zap/zap-baseline-retest-metadata-2026-06-14.md`, `.json`, `.html` | No cookies, tokens, content, or secrets committed. |
| OWASP-2026-06-14-013 | 2026-06-14 | Production public URL after restart, before Cloudflare cache purge | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-metadata-final-2026-06-14.html -J zap-baseline-retest-metadata-final-2026-06-14.json -w zap-baseline-retest-metadata-final-2026-06-14.md -I`; Python stdlib metadata samples with `Cache-Control: no-cache`. | Anonymous/public. | Blocked by stale Cloudflare cache. `/robots.txt` still returned cached `Allow: /$` with `cf-cache-status: HIT`; ZAP still crawled literal `/$`. `security.txt`, `sitemap.xml`, HSTS, CSRF, Permissions-Policy, and COEP remained correct. | `07-tool-outputs/zap/zap-baseline-retest-metadata-final-2026-06-14.md`, `.json`, `.html` | Purge Cloudflare cache for `/robots.txt` or wait TTL, then rerun. No cookies, tokens, content, or secrets committed. |
| OWASP-2026-06-14-013 | 2026-06-14 | Production public URL after `/robots.txt` Cloudflare cache purge | `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-metadata-after-purge-2026-06-14.html -J zap-baseline-retest-metadata-after-purge-2026-06-14.json -w zap-baseline-retest-metadata-after-purge-2026-06-14.md -I`; Python stdlib `/robots.txt` sample. | Anonymous/public. | Passed for metadata finding. `/robots.txt` returned current `Allow: /` with no cookie; ZAP no longer crawled literal `/$`. `security.txt` and intentional `sitemap.xml` 404 remained published from prior retest; HSTS, CSRF, Permissions-Policy, and COEP checks passed. | `07-tool-outputs/zap/zap-baseline-retest-metadata-after-purge-2026-06-14.md`, `.json`, `.html` | Remaining warnings are separate accepted/open work items: readable CSRF cookie design, cache-control informational warnings, session-management/authentication identification. No cookies, tokens, content, or secrets committed. |
| OWASP-2026-06-14-014 | 2026-06-14 | Local/test security regression suite plus production ZAP warning context | `.venv/bin/pytest -q tests/test_cookie_csrf_security.py -k "cookie or csrf_cookie or trusted_device"`; production ZAP baseline warnings from `zap-baseline-retest-metadata-after-purge-2026-06-14.*`. | Synthetic local users only; public ZAP unauthenticated context. | Accepted design warning. Tests verify `openscribe_csrf` is readable and signed, anonymous nonce cookie is `HttpOnly`, session cookie is `HttpOnly`, trusted-device cookie is `HttpOnly`, and CSRF cookie alone does not authenticate `/api/v1/auth/me`. Existing API tests verify missing/mismatched/cross-origin CSRF rejection for cookie-authenticated unsafe API requests. | `tests/test_cookie_csrf_security.py`; `tests/test_api.py`; latest ZAP output `07-tool-outputs/zap/zap-baseline-retest-metadata-after-purge-2026-06-14.*` | `openscribe_csrf` is not an auth bearer secret. Keep XSS/CSP controls strong because readable CSRF token is intentionally script-readable for same-origin browser API calls. |
| OWASP-2026-06-14-015 | 2026-06-14 | Local/test cache-header regression suite | `.venv/bin/pytest -q tests/test_cookie_csrf_security.py -k "cache or metadata or static_assets or no_store"`; `.venv/bin/pytest -q tests/test_cookie_csrf_security.py`; `.venv/bin/python -m py_compile app/main.py` | Synthetic local only; no user content. | Passed locally. Tests verify `/`, public auth pages, and `/api`/`/api/` are `no-store` with legacy cache-busting headers; metadata routes and static assets are cookie-free with `Cache-Control: public, max-age=3600`. | `tests/test_cookie_csrf_security.py` | Superseded by production retest row below. Static assets and metadata are public, non-content routes; short public caching is intentional. |
| OWASP-2026-06-14-015 | 2026-06-14 | Production public URL after cache-control deploy/restart | Browser-UA Python stdlib header sample; `docker run --rm -t -v ".../07-tool-outputs/zap:/zap/wrk:rw" zaproxy/zap-stable zap-baseline.py -t "https://openscribe.co.uk" -m 1 -r zap-baseline-retest-cache-2026-06-14.html -J zap-baseline-retest-cache-2026-06-14.json -w zap-baseline-retest-cache-2026-06-14.md -I` | Anonymous/public; ZAP default synthetic form values only. | Accepted. Dynamic/root/auth/API samples are `no-store`; metadata/static samples are cookie-free public cache. ZAP still reports cache informational alerts, but `/api/` no longer appears under `Storable and Cacheable Content`; that alert is now limited to public `robots.txt` and intentional `sitemap.xml` 404. `Retrieved from Cache` is limited to public `robots.txt` and static JS. `Re-examine Cache-control Directives` flags `no-store` on root/auth and public cache on `robots.txt`; `Non-Storable Content` flags auth redirects that should not cache. | `07-tool-outputs/cache-header-samples-2026-06-14.txt`; `07-tool-outputs/zap/zap-baseline-retest-cache-2026-06-14.md`, `.json`, `.html` | Residual cache warnings are accepted design/heuristic alerts, not content leakage. No cookies, tokens, account data, transcript/note content, prompts, provider responses, or audio committed. |
| OWASP-2026-06-14-016 | 2026-06-14 | Production ZAP warning context plus local/test security regression suite | ZAP context from `zap-baseline-retest-cache-2026-06-14.*`; `.venv/bin/pytest -q tests/test_cookie_csrf_security.py -k "csrf_cookie_alone or session_cookie or trusted_device or anon_nonce"`; `.venv/bin/pytest -q tests/test_api.py -k "csrf"` | Synthetic local users only; public ZAP unauthenticated context. | Accepted. ZAP identified expected login form fields and anonymous CSRF cookies. Tests verify CSRF cookies alone do not authenticate `/api/v1/auth/me`, `openscribe_session` is `HttpOnly`, `openscribe_trusted_device` is `HttpOnly`, anonymous nonce is `HttpOnly`, and unsafe cookie-authenticated API requests require same-origin CSRF proof. | `07-tool-outputs/session-scanner-triage-2026-06-14.txt`; `07-tool-outputs/zap/zap-baseline-retest-cache-2026-06-14.md`, `.json`, `.html`; `tests/test_cookie_csrf_security.py`; `tests/test_api.py` | `Authentication Request Identified` and `Session Management Response Identified` are scanner auto-detection notices, not vulnerabilities. No cookie values, tokens, account data, transcript/note content, prompts, provider responses, or audio committed. |
| OWASP-2026-06-14-006 | 2026-06-14 | Local `.venv` dependency scan | `.venv/bin/pip-audit -r requirements.txt --desc on`; `.venv/bin/pip-audit --desc on`; `.venv/bin/pip install -r requirements.txt --upgrade`; rerun `pip-audit` to confirm fixes. | N/A (package-level scan only). | Resolved for direct dependencies. Upgraded: cryptography 46.0.3→46.0.7 (3 vulns fixed), requests 2.32.5→2.33.0 (1 vuln fixed), python-multipart 0.0.22→0.0.32 (2 vulns fixed), idna 3.11→3.18 (1 vuln fixed). Remaining: 5 vulns in starlette (FastAPI-constrained, →017) and pytest (dev-only, accepted). Static vendor assets and Docker images inventoried; no known CVEs at current versions. | `07-tool-outputs/sbom-pip-audit-2026-06-14.md`; `requirements.txt` | No cookies, tokens, account data, transcript/note content, prompts, provider responses, or audio committed. |
| OWASP-2026-06-14-007 | 2026-06-14 | Existing production captures consolidated into formal evidence file | Production ZAP baseline retests (HSTS, Permissions-Policy, COEP, CSRF headers); Python stdlib header/cache captures; HackerTarget DNS/HTTP header passive recon; local regression test suite (`tests/test_cookie_csrf_security.py`). | Anonymous/public captures; synthetic local tests. | Completed. All 12 hardening headers confirmed present in production via prior ZAP retests. Cookie contract verified by regression tests (26 + 7 passed). Cache-control policy confirmed by route class. TLS 1.3 with Let's Encrypt certificate confirmed. | `07-tool-outputs/tls-header-cookie-evidence-2026-06-14.md`; `05-server-fingerprinting.md`; `07-tool-outputs/zap/zap-baseline-retest-*.md`; `07-tool-outputs/cache-header-samples-2026-06-14.txt` | Evidence consolidated from prior retests; no new production capture needed. No cookies values, tokens, or secrets committed. |
| OWASP-2026-06-14-017 | 2026-06-14 | Local `.venv` FastAPI/starlette upgrade | `.venv/bin/pip install "fastapi[standard]>=0.137.0"`; `.venv/bin/pip-audit` (both modes); `.venv/bin/pytest -q tests/test_cookie_csrf_security.py tests/test_api.py -k "csrf or auth"`; `.venv/bin/pytest -q tests/test_api.py::test_transcribe_workspace_stream_route_does_not_depend_on_request_scoped_db_session` | Synthetic local tests. | Resolved. FastAPI upgraded 0.115.0→0.137.0; starlette upgraded 0.38.6→1.3.1. pip-audit requirements.txt now shows only 1 vuln (pytest, dev-only); installed audit shows no starlette vulns. Security tests: 44/44 passed. API smoke: 278 passed. Fixed 1 test for starlette 1.x route lookup change (api.routes vs app.routes). | `requirements.txt`; `tests/test_api.py`; `07-tool-outputs/sbom-pip-audit-2026-06-14.md` | Only deprecation warnings (httpx→httpx2 testclient, HTTP_422 constant rename). No breaking changes. No cookies, tokens, account data, or secrets committed. |
| OWASP-2026-06-14-003 | 2026-06-14 | Local code review + unit tests | XSS surface analysis: 19 templates, all JS innerHTML sites, CSP config, Jinja2 auto-escaping. Fixed 2 XSS bugs. `.venv/bin/pytest -q tests/test_xss_coverage.py -v` | Synthetic local only. | Closed. Bugs fixed: (1) `structured.js:1232` — `generatedDocument.error_message` unescaped in innerHTML → added `escapeHtml()` wrapper. (2) `_workspace.html:359` — `|tojson` without `|forceescape` in single-quoted attribute → added `|forceescape`. 28 new XSS coverage tests pass. Zero `|safe` filter usages. CSP `script-src-attr 'none'` verified. All innerHTML sites verified to use `escapeHtml()`. | `app/static/js/transcribe/structured.js`; `app/templates/transcribe/_workspace.html`; `tests/test_xss_coverage.py` | No production capture needed. XSS bugs fixed and verified via static analysis + unit tests. No cookies, tokens, or secrets committed. |
| OWASP-2026-06-14-004 | 2026-06-14 | Local schema validation tests | `.venv/bin/pytest -q tests/test_ssrf_canary.py -v`; URL validation schemas (stt.py, llm.py, deidentification.py) tested for scheme/host/private-IP rules. | Synthetic local only. | Closed. 23 SSRF canary tests pass. Verified: URL validation rejects remote HTTP, accepts valid HTTPS, allows localhost/private HTTP by design (for Ollama/Presidio). Auth required: inspect endpoints return 401/403 unauthenticated. httpx defaults to `follow_redirects=False` (no redirect SSRF). No host allow/block list by design (system admins configure providers). Recommended future hardening: add metadata-service IP blocklist. | `tests/test_ssrf_canary.py`; `app/schemas/stt.py`; `app/schemas/llm.py`; `app/schemas/deidentification.py` | No production capture needed. Schema-level tests verify URL validation contract. No cookies, tokens, or secrets committed. |
| OWASP-2026-06-14-008 | 2026-06-14 | Documentation | AI safety threat model covering 6 threat cases (prompt injection, hallucination, data leakage, redaction failure, malicious model, model DoS). Mitigations mapped per threat. Acceptance criteria documented. | N/A (documentation). | Closed. `/docs/Compliance/OWASP/security-evidence/owasp/2026-06-14/08-ai-safety-plan.md` written. Threat cases: T-001 through T-006. Structured note JSON contract validated per AGENTS.md. | `08-ai-safety-plan.md` | No production capture needed. Documentation only. No cookies, tokens, or secrets committed. |
| OWASP-2026-06-14-002 | 2026-06-15 | Local dev app: `http://127.0.0.1:8080` | OWASP ZAP `2.17.0` daemon proxy on Docker host network; safe GET-only Python client using API login and in-memory cookie replay. | Seeded dev normal user and seeded dev team leader; anonymous negative checks. | Partial. 95 ZAP messages, 46 unique paths, 75 role requests. Anonymous protected routes redirected/401 as expected. Normal user content/personal surfaces allowed and admin/provider/team-management surfaces denied. Team leader own-team metadata/config surfaces allowed and system/provider config lists denied. Onboarding-only, pending-MFA, and system-admin sessions not covered. | `06-proxy-crawl-summary.md`; `07-tool-outputs/zap/zap-auth-crawl-local-2026-06-15-summary.md` | No cookies, CSRF tokens, passwords, session tokens, transcript/note content, prompts, provider responses, or audio committed. Login throttle key was cleared in local Redis after failed setup attempts; no app DB/content rows cleared. |
