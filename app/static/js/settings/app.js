function snapshot(form) {
  const data = new FormData(form); data.delete('_csrf_token'); return new URLSearchParams(data).toString();
}
document.querySelectorAll('[data-confirm-submit]').forEach((form) => form.addEventListener('submit', (event) => { if (!window.confirm(form.dataset.confirmSubmit)) event.preventDefault(); }));
document.querySelectorAll('[data-service-toggle]').forEach((button) => button.addEventListener('click', () => { const body = document.querySelector(`[data-service-body="${CSS.escape(button.dataset.serviceToggle || '')}"]`); if (!body) return; body.hidden = !body.hidden; button.textContent = body.hidden ? 'Configure' : 'Close'; }));
document.querySelectorAll('[data-stt-selection-form]').forEach((form) => { const config = form.querySelector('[data-stt-config-select]'); const model = form.querySelector('[data-stt-model-select]'); const sync = () => { if (!config || !model) return; const option = config.options[config.selectedIndex]; let models = []; try { models = JSON.parse(option?.dataset.models || '[]'); } catch (_) {} const selected = model.dataset.selectedValue || ''; model.replaceChildren(new Option('Use endpoint default', '')); models.forEach((value) => model.add(new Option(value, value))); model.value = models.includes(selected) ? selected : (models.includes(option?.dataset.defaultModel) ? option.dataset.defaultModel : ''); }; config?.addEventListener('change', sync); sync(); });
document.querySelectorAll('[data-llm-selection-form]').forEach((form) => { const select = form.querySelector('[data-llm-config-select]'); const sync = () => form.querySelectorAll('[data-llm-model-group]').forEach((group) => { const active = group.dataset.llmModelGroup === select?.value; group.hidden = !active; group.querySelectorAll('input').forEach((input) => { input.disabled = !active; }); }); select?.addEventListener('change', sync); sync(); });
document.querySelector('[data-recovery-copy]')?.addEventListener('click', async () => { const input = document.querySelector('[data-recovery-temp-password]'); const status = document.querySelector('[data-recovery-copy-status]'); if (!input || !status) return; try { await navigator.clipboard.writeText(input.value); } catch (_) { input.select(); document.execCommand('copy'); } status.textContent = 'Copied'; });
document.querySelectorAll('[data-dirty-guard]').forEach((form) => { const initial = snapshot(form); let submitting = false; form.addEventListener('submit', () => { submitting = true; }); window.addEventListener('beforeunload', (event) => { if (submitting || snapshot(form) === initial) return; event.preventDefault(); event.returnValue = ''; }); });
window.lucide?.createIcons();
